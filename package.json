{
  "name": "vax360",
  "version": "3.0.0",
  "description": "Vax360 – Track and manage your children's vaccination schedule",
  "private": true,
  "scripts": {
    "dev": "npx serve . -l 3000",
    "dev:backend": "cd backend && node server.js",
    "dev:all": "npm run dev & npm run dev:backend",
    "db:init": "cd backend && node db-init.js",
    "db:reset": "cd backend && rm -f database.sqlite && node db-init.js",
    "setup": "npm install && cd backend && npm install && cd .. && npm run db:init",
    "build": "echo 'Static PWA — no build step needed. Files are ready in ./'",
    "cap:init": "npx -y @capacitor/cli init Vax360 com.vax360.app --web-dir .",
    "cap:add:android": "npx -y @capacitor/cli add android",
    "cap:add:ios": "npx -y @capacitor/cli add ios",
    "cap:sync": "npx -y @capacitor/cli sync",
    "cap:open:android": "npx -y @capacitor/cli open android",
    "cap:open:ios": "npx -y @capacitor/cli open ios",
    "android": "npm run cap:sync && npm run cap:open:android",
    "ios": "npm run cap:sync && npm run cap:open:ios",
    "test": "mocha tests/**/*.test.js"
  },
  "dependencies": {
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/haptics": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "chai": "^6.2.2",
    "jsdom": "^29.0.2",
    "jsdom-global": "^3.0.2",
    "mocha": "^11.7.5"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "Vax360 Team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/igdplda-netizen/vax360-app.git"
  },
  "main": "app.js",
  "keywords": [],
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/igdplda-netizen/vax360-app/issues"
  },
  "homepage": "https://github.com/igdplda-netizen/vax360-app#readme"
}
